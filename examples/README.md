# Examples

No production-derived adapters ship in this repo, and none are planned as
a separate public package — see `ADAPTERS.md` for why.

This folder is a placeholder for minimal, engine-only usage snippets (e.g.
"here's a 20-line HTTP scraper using just `runWorkerPool` + `transports/http`")
once the API has settled. Not yet populated.
