# Examples

Working, runnable adapters (Google, MagicPin, Zomato) live in the sibling
`scraper-adapters` repo/package, not here — see `OSS_PLAN.md` §3 for why
they're kept separate from the harness.

This folder is a placeholder for minimal, harness-only usage snippets (e.g.
"here's a 20-line HTTP scraper using just `runWorkerPool` + `transports/http`")
once the harness API has settled. Not yet populated.
